#include<stdio.h>
int main()
{
    int n,m,sum=0,b;
    int a[101];
    while(scanf("%d %d",&n,&m)!=EOF)
    {
        b = n%m;
        if(b==0)
        {
            printf("")
        }    
    
    }
}