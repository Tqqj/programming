#include<stdio.h>
struct Data{
    int year;
    int month;
    int day;
};
int main()
{
    int sum;
    struct Data a,b;
    scanf("%d %d %d",a.year,a.month,a.day);
    scanf("%d %d %d",b.year,b.month,b.day);
    if(a.year%400==0||a.year%4==0&&a.year)

}